interface ProgressBarProps {
  value?: any
  max?: any
  showLabel?: any
}

export default function ProgressBar({ value, max, showLabel }: ProgressBarProps) {
  return (
    <div className="">
      {/* ProgressBar component content */}
    </div>
  )
}